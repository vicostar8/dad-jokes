import React from "react";

const Phone_BottomBar_Reset = ({ resetPanel }) => {
  return (
    <div className="Phone_BottomBar_Buttons" onClick={resetPanel}>
      RESET
    </div>
  );
};

export default Phone_BottomBar_Reset;
