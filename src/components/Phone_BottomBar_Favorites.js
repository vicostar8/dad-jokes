import React from "react";

const Phone_BottomBar_Favorites = ({ toggleView }) => {
  return (
    <div className="Phone_BottomBar_Buttons" onClick={toggleView}>
      Favorites
    </div>
  );
};

export default Phone_BottomBar_Favorites;
